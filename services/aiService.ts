
import { Visitor } from "../types";

/**
 * AI xizmati foydalanuvchi so'roviga binoan o'chirildi.
 */
export const getVisitorInsights = async (visitors: Visitor[]): Promise<string> => {
  return "";
};
